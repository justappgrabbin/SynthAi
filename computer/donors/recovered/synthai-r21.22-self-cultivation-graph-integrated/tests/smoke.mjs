import assert from 'node:assert/strict';import {SynthiaBridge} from '../canonical-address-runtime/synthia-bridge.js';import {GraphRuntime} from '../runtime/GraphRuntime.js';import {installSynthiaToolStack} from '../UPGRADES/bootstrap/SynthiaToolBootstrap.js';import OrganRegistry from '../core/OrganRegistry.mjs';import WorldOrgan from '../organs/WorldOrgan.js';import EconomyOrgan from '../organs/EconomyOrgan.js';import BuilderOrgan from '../organs/BuilderOrgan.js';import ResonanceOrgan from '../organs/ResonanceOrgan.js';import {STRUCTURES} from '../vendor/ato-core/src/activation.mjs';
const bridge=new SynthiaBridge();const addr=bridge.process({gate:0,line:0,color:0,tone:0,base:0},{planet:0,dimension:0,zodiac:0,house:0});assert.equal(addr.micro.gate,0);assert.equal(STRUCTURES.decagram.canonicalActive,8);const rt=new GraphRuntime(),stack=installSynthiaToolStack(rt);assert.ok(rt.getRegisteredTools().length>=10);const s=await rt.ingest({intentId:'smoke',description:'build a small greeting tool',side:'FIVE_SIDE',planet:1,dimension:1,seed:1n});for(let i=0;i<5;i++)await rt.step(s.sessionId);assert.ok(rt.getRegisteredTools().some(t=>t.toolId.includes('autoling')));const reg=new OrganRegistry();reg.register(new WorldOrgan());reg.register(new EconomyOrgan());reg.register(new BuilderOrgan());reg.register(new ResonanceOrgan());assert.equal(reg.list().length,4);const app=await reg.run('builder',{intent:'make a game',address:addr});assert.match(app.artifact.content,/<!doctype html>/i);const res=await reg.run('resonance',{gates:[1,2,6]});assert.equal(res.ok,true);console.log(JSON.stringify({ok:true,tools:rt.getRegisteredTools().length,organs:reg.list().length,decagram:STRUCTURES.decagram,address:addr.micro},null,2));