import test from'node:test';import assert from'node:assert/strict';import{ComputerRuntime}from'../ComputerRuntime.mjs';import{MemoryPersistence}from'../core/kernel.mjs';
test('boot macros micros',async()=>{const c=await new ComputerRuntime().boot();assert.equal(c.systems.get('synthai-computer').role,'os');assert.equal(c.systems.get('synthai2').role,'browser');assert.equal(c.systems.get('xynthai').audience,'18+');assert.equal(c.micros.get('venom').macro,null)});
test('intake mutation mount persistence',async()=>{const persistence=new MemoryPersistence(),c=await new ComputerRuntime({persistence}).boot(),a=await c.intake.ingest({name:'demo.html',text:'<h1>demo</h1>'}),p=c.intake.proposeMount(a.id,{appId:'demo-app'}),applied=await c.mutations.apply(p.id);assert.equal(applied.status,'applied');assert.equal(c.shellManager.listMounted()[0].appId,'demo-app');assert.match(c.vfs.read(a.path).content,/demo/);const restored=await new ComputerRuntime({persistence}).boot();assert.equal(restored.state.get(`mutations.${p.id}`).status,'applied')});
test('mutation failure surfaces',async()=>{const c=await new ComputerRuntime().boot(),p=c.mutations.propose({actions:[{type:'nope'}]});await assert.rejects(()=>c.mutations.apply(p.id),/unhandled mutation action/);assert.equal(c.state.get(`mutations.${p.id}`).status,'failed')});
test('automata and process use real inputs',async()=>{const c=await new ComputerRuntime().boot();c.automata.register({id:'echo',execute:async({input,state})=>{await state.set('test.input',input,{source:'echo'});return input}});assert.equal((await c.automata.run('echo',{message:'runtime'})).message,'runtime');c.processes.register({id:'normalize',run:async({input})=>String(input).trim().toLowerCase()});assert.equal(await c.processes.execute('normalize',' HELLO '),'hello')});
test('cultivation persists',async()=>{const c=await new ComputerRuntime().boot(),x=await c.cultivation.begin({goal:'learn'});await c.cultivation.observe(x.id,{noticed:'gap'});await c.cultivation.act(x.id,{do:'practice'});assert.equal((await c.cultivation.integrate(x.id,{result:'learned'})).status,'complete')});
test('xynthai boundary',async()=>{const c=await new ComputerRuntime().boot();await assert.rejects(()=>c.policy.assert('xynthai:age-boundary',{ageConfirmed:false}),/18\+/);assert.equal((await c.policy.assert('xynthai:age-boundary',{ageConfirmed:true})).allow,true)});
