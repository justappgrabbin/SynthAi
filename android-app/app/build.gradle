plugins {
    id 'com.android.application'
}

android {
    namespace 'app.synthai.computer'
    compileSdk 35

    defaultConfig {
        applicationId 'app.synthai.computer'
        minSdk 26
        targetSdk 35
        versionCode 2
        versionName '0.4.0'
    }

    buildTypes {
        release {
            minifyEnabled false
        }
        debug {
            debuggable true
        }
    }

    androidResources {
        noCompress += ['tar.gz']
    }

    packaging {
        jniLibs {
            useLegacyPackaging = true
        }
    }
}

dependencies {
    implementation 'org.apache.commons:commons-compress:1.27.1'
}
