# GitHub-only SynthAI app build

This branch builds the preserved repository Computer surface into a browser/PWA host and an Android debug APK. The workflow must pass repository tests before packaging.
