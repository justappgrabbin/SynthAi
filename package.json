{
  "name": "synthai-computer-modular-monolith",
  "private": true,
  "type": "module",
  "version": "0.3.0-mobile",
  "scripts": {
    "test:computer": "node --test computer/tests/*.test.mjs",
    "build:computer": "node computer/build-web.mjs",
    "android:init": "npm run build:computer && npx cap add android",
    "android:sync": "npm run build:computer && npx cap sync android",
    "android:debug": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "test": "npm run test:computer && npm run build:computer"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
